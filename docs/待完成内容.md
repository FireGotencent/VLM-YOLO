# 待完成内容（TODO）

> 生成时间：2026-01-30  
> 说明：基于当前仓库结构（桌面端 PyQt6、服务端 WebSocket、移动端 Kivy）整理的待办清单，供后续迭代排期使用。

## P0（优先：影响可用性/可维护性）

- [ ] **确认模型权重文件策略**：当前 `yolo26n.pt` / `yolov8n.pt` 位于仓库根目录，但 `.gitignore` 仅忽略 `models/*.pt`；建议统一放入 `models/`（并继续忽略提交）或补充下载脚本/忽略规则，避免仓库体积膨胀。
- [ ] **服务端读取配置文件**：`server/config.yaml` 已存在，但 `server/websocket_server.py` 仍以硬编码参数启动（host/port/model/processing/use_llm 等）；改为启动时加载配置并生效。
- [ ] **模型路径解析更稳健**：`YOLODetector(model_path="yolo26n.pt")` 依赖当前工作目录；建议统一将相对路径解析到项目根目录（桌面端与服务端都适用），并在缺失权重时给出清晰错误提示。
- [ ] **WebSocket 协议容错与限流**：对非法 JSON/字段缺失/超大帧给出明确错误响应与日志；移动端 `mobile/websocket_client.py` 的 `_send_queue` 建议增加队列上限与丢帧策略，避免弱网或服务端变慢时积压导致延迟飙升。

## P1（功能增强：体验/效果/性能）

- [ ] **距离估计**：`src/fusion/scene_encoder.py` 支持 `distances` 输入，但当前链路未提供距离来源；补充单目深度/标定+目标尺寸估算等距离模块，并将真实距离接入 `NavigationAdvisor.generate_warning` 的紧急程度判定。
- [ ] **危险等级策略升级**：`SceneEncoder.DANGER_LEVELS` 目前仅按类别固定；建议结合距离、目标面积变化（近似相对速度）与方位，输出更稳定的风险分级。
- [ ] **语言与输出一致性**：`NavigationAdvisor.quick_alert`、服务端 `tts_text`、桌面端 UI 与移动端显示统一中文类名（复用 `YOLODetector.get_class_name_zh`），并确保 `config.yaml` 的 `detection.classes` 与输出策略一致。
- [ ] **桌面端参数可配置**：在 `src/ui/components/control_panel.py` 增加模型路径、置信度、IOU、设备、目标类别、语音播报频率等控制项，并持久化回 `config.yaml`。
- [ ] **性能与可观测性**：桌面端/服务端增加推理耗时、端到端延迟、平均 FPS 指标展示；必要时加入帧跳过、异步推理或多线程流水线以提升实时性。
- [ ] **移动端传输参数可配置**：将发送 FPS、JPEG quality、分辨率等从硬编码改为可配置并保存，最好与 `server/config.yaml` 的 `processing`（target_fps/jpeg_quality/max_frame_size）对齐或进行协商。
- [ ] **服务端多客户端策略**：让 `server/config.yaml` 的 `max_clients` 真正生效，并明确策略（逐客户端返回/广播、超限拒绝、统计与日志）。

## P2（工程化：测试/CI/发布/文档）

- [ ] **补齐测试**：仓库已依赖 `pytest`，但 `tests/` 仅有 `__init__.py`；补充配置加载、场景摘要、导航降级逻辑、服务端帧解析与响应格式的单元/最小集成测试。
- [ ] **CI 自动化**：添加基础流水线（lint/format 可选，至少运行 `pytest`），并在 PR/提交时自动执行。
- [ ] **安装与常见问题文档**：补充 Windows 下 `pyaudio`、`PyQt6`、`opencv-python` 等依赖的安装/排错说明，以及 `.env`/API Key 配置示例。
- [ ] **打包与版本管理**：桌面端（如 PyInstaller）与移动端（buildozer/Colab）的构建流程固化；增加版本号与变更记录（CHANGELOG）。

## 可选（安全与隐私）

- [ ] **鉴权与加密**：WebSocket 增加 token 鉴权；需要公网/不依赖 Tailscale 时支持 TLS（wss）。
- [ ] **隐私默认值**：默认不落盘保存视频帧；日志中避免记录敏感信息，提供开关与脱敏策略。

