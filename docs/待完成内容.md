# 待完成内容（TODO）

> 说明：基于当前仓库结构（服务端 WebSocket + Web 客户端 PWA）整理的待办清单。

## P0（优先：影响可用性/可维护性）

- [ ] **确认模型权重文件策略**：当前 `yolo26n.pt` / `yolov8n.pt` 位于仓库根目录，但 `.gitignore` 仅忽略 `models/*.pt`；建议统一放入 `models/`（并继续忽略提交）或补充下载脚本/忽略规则，避免仓库体积膨胀。
- [ ] **模型路径解析更稳健**：`YOLODetector(model_path="yolo26n.pt")` 依赖当前工作目录；建议统一将相对路径解析到项目根目录，并在缺失权重时给出清晰错误提示。
- [ ] **WebSocket 协议容错与限流**：对非法 JSON/字段缺失/超大帧给出明确错误响应与日志；服务端增加队列上限与丢帧策略，避免弱网时积压导致延迟飙升。

## P1（功能增强：体验/效果/性能）

- [ ] **视觉多模态**：LLM Provider 已实现 `chat_with_vision()`，但服务端未调用图像数据；将解码后的帧传给 LLM 做真正的图像理解，而不仅依赖 YOLO 文字描述。
- [ ] **距离估计**：当前基于目标框面积的粗略启发式估算；补充单目深度/标定+目标尺寸估算，并将真实距离接入 `NavigationAdvisor.generate_warning` 的紧急程度判定。
- [ ] **Web 客户端检测框可视化**：当前仅文字列出检测结果，未在视频画面上绘制 bounding box；在 canvas 上叠加绘制。
- [ ] **Web 客户端设置持久化**：服务器地址、FPS、JPEG 质量等设置刷新后丢失，需加 localStorage。
- [ ] **危险等级策略升级**：`SceneEncoder.DANGER_LEVELS` 目前仅按类别固定；建议结合距离、目标面积变化与方位，输出更稳定的风险分级。
- [ ] **服务端多客户端策略**：让 `server/config.yaml` 的 `max_clients` 真正生效，并明确策略（逐客户端返回/广播、超限拒绝、统计与日志）。
- [ ] **性能与可观测性**：服务端增加推理耗时、端到端延迟、平均 FPS 指标展示；必要时加入帧跳过、异步推理以提升实时性。

## P2（工程化：测试/CI/发布/文档）

- [ ] **补齐测试**：仓库已依赖 `pytest`，但 `tests/` 仅有 `__init__.py`；补充配置加载、场景摘要、导航降级逻辑、服务端帧解析与响应格式的单元/最小集成测试。
- [ ] **CI 自动化**：添加基础流水线（lint/format 可选，至少运行 `pytest`），并在 PR/提交时自动执行。
- [ ] **安装与常见问题文档**：补充 Windows 下 `pyaudio`、`PyQt6`、`opencv-python` 等依赖的安装/排错说明，以及 `.env`/API Key 配置示例。

## 可选（安全与隐私）

- [ ] **鉴权**：WebSocket 增加 token 鉴权，避免服务对所有连接开放。
- [ ] **隐私默认值**：默认不落盘保存视频帧；日志中避免记录敏感信息，提供开关与脱敏策略。
