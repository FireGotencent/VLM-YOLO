# VisionGuide LLM System

基于多模态大模型与YOLO目标检测的盲人避障导航系统

## 项目概述

本系统为视障人士提供一套低成本、高效率的智能导航解决方案。通过实时检测周围环境中的障碍物并以自然语言形式进行语音提醒，可以显著提升视障人士的出行安全性和独立性。

## 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                      用户交互层                               │
│           语音输入 / 语音输出 / 图形界面                        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      智能决策层                               │
│              多模态大模型推理引擎 (LLM/VLM)                    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      感知融合层                               │
│           视觉-语言对齐模块 / 空间信息编码模块                   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      感知识别层                               │
│             YOLO目标检测模块 / 深度估计模块                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      数据采集层                               │
│                  摄像头/深度相机数据采集                        │
└─────────────────────────────────────────────────────────────┘
```

## 核心特性

- 🎯 **实时目标检测**: 基于 YOLOv8 的高效障碍物检测
- 🧠 **智能场景理解**: 多模态大模型进行语义推理和导航建议
- 🔊 **语音交互**: 自然语言语音提醒，支持语音指令输入
- 🔌 **灵活部署**: 支持本地 Ollama 和云端 API 双模式

## 快速开始

### 环境配置

```bash
# 激活虚拟环境
conda activate vgllm

# 安装依赖
pip install -r requirements.txt
```

### OpenAI 配置

- 桌面端配置位置：`config.yaml` → `llm.provider` / `llm.openai.base_url`
- 服务端配置位置：`server/config.yaml` → `llm.provider` / `llm.model` / `llm.base_url` / `llm.use_llm`
- API Key 配置位置：复制 `.env.example` 为 `.env`，设置 `OPENAI_API_KEY=...`（不要提交到仓库）

### 运行程序

```bash
python main.py
```

### 网页版客户端（免 APK 联调）

> 适用于快速迭代移动端逻辑，不需要每次重新打包安装 APK；支持安装为 PWA。

1. 启动服务端（接收视频帧并返回检测结果）：

```bash
python -m server.websocket_server
```

2. 启动静态文件服务（在项目根目录执行）：

```bash
python -m http.server 8081 --directory mobile
```

3. 浏览器打开：

```text
http://100.111.15.26:8081/web_client.html?ws=ws%3A%2F%2F100.111.15.26%3A8765

```

4. 页面中连接 `ws://127.0.0.1:8765`，开启摄像头后即可联调。

5. 安装为 PWA（可选）：
- 在页面点击“安装到主屏幕”
- 或使用浏览器菜单“添加到主屏幕/安装应用”

说明：
- 手机浏览器通常要求 `HTTPS` 才允许摄像头；本地电脑调试可直接使用 `localhost`。
- 若页面是 `https`，WebSocket 也应使用 `wss`。
- PWA 资源文件：`mobile/manifest.webmanifest`、`mobile/sw.js`、`mobile/icons/`

## 目录结构

```
VisionGuide_LLM_System/
├── src/                    # 源代码
│   ├── capture/            # 数据采集层
│   ├── detection/          # 感知识别层
│   ├── fusion/             # 感知融合层
│   ├── reasoning/          # 智能决策层
│   ├── interaction/        # 用户交互层
│   ├── ui/                 # 图形界面
│   └── utils/              # 工具模块
├── models/                 # 模型文件
├── tests/                  # 测试代码
├── docs/                   # 文档
├── config.yaml             # 配置文件
├── requirements.txt        # 依赖列表
└── main.py                 # 程序入口
```

## 技术栈

- **目标检测**: YOLOv8 / Ultralytics
- **大语言模型**: Ollama / OpenAI / Claude / Gemini
- **语音合成**: pyttsx3
- **语音识别**: SpeechRecognition
- **图形界面**: PyQt6
- **数据处理**: OpenCV, NumPy

## 许可证

MIT License
